{% extends 'base.html.twig' %}

{% block title %}
    AFA-Manager
{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block body %}
    <h1>Liste des modifications en attente</h1>
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">Prénom</th>
                <th scope="col">Nom</th>
                <th scope="col">Date de naissance</th>
                <th scope="col">Adresse</th>
                <th scope="col">Code postal</th>
                <th scope="col">Localité</th>
                <th scope="col">Pays</th>
                <th scope="col">Email</th>
                <th scope="col">Téléphone</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
        {% for modification in modifications %}
            <tr>
                <td>{{ modification.FirstName|title }} ({{ modification.NewFirstName }})</td>
                <td>{{ modification.Name|title }} ({{ modification.NewName }})</td>
                <td>{% if modification.Birthday != null %}{{ modification.Birthday|date("d/m/Y") }}{% endif %}</td>
                <td>{{ modification.Address }}</td>
                <td>{{ modification.Zip }}</td>
                <td>{{ modification.City|title }}</td>
                <td>{{ modification.Country }}</td>
                <td>{{ modification.Email }}</td>
                <td>{{ modification.Phone }}</td>
                <td><a class="btn btn-primary" href="{{ path('secretariat_member_modification_validation_validate', {'member': modification.Member}) }}">Valider</a><a class="btn btn-primary" href="{{ path('secretariat_member_modification_validation_cancel', {'member': modification.Member}) }}">Refuser</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block javascripts %}
{% endblock %}
