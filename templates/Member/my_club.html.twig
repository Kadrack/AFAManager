{% extends 'base.html.twig' %}

{% block title %}
    AFA-Manager
{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block body %}
    <h1>{{ clubTools.getClub.getClubName()|title }} n°{{ clubTools.getClub.getClubId() }}</h1>
    {% if clubTools.getLessonsDetails().Dojos != null %}
    <h2>Dojo(s)</h2>
    <table class="table table-sm table-hover table-striped">
        <thead>
            <tr>
                <th scope="col">Salle</th>
                <th scope="col">Adresse</th>
                <th scope="col">Code postal</th>
                <th scope="col">Localité</th>
                <th scope="col">Tatamis (m²)</th>
                <th scope="col">DEA</th>
            </tr>
        </thead>
        <tbody>
        {% for dojo in clubTools.getLessonsDetails().Dojos %}
            <tr>
                <td class="align-middle">{{ dojo.getTrainingAddressName() }}</td>
                <td class="align-middle">{{ dojo.getTrainingAddressStreet() }}</td>
                <td class="align-middle">{{ dojo.getTrainingAddressZip() }}</td>
                <td class="align-middle">{{ dojo.getTrainingAddressCity() }}</td>
                <td class="align-middle">{{ dojo.getTrainingAddressTatamis() }}</td>
                <td class="align-middle">{% if dojo.getTrainingAddressDEA() %}Oui{% else %}Non{% endif %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% endif %}
    {% if clubTools.getLessonsDetails().Trainings != null %}
    <h2>Horaire cours</h2>
    <table class="table table-sm table-hover table-striped">
        <thead>
            <tr>
                <th scope="col">Jour</th>
                <th scope="col">Début</th>
                <th scope="col">Fin</th>
                <th scope="col">Type</th>
                <th scope="col">Salle</th>
            </tr>
        </thead>
        <tbody>
        {% for training in clubTools.getLessonsDetails().Trainings %}
            <tr>
                <td class="align-middle">{{ listData.getDay(training.getTrainingDay()) }}</td>
                <td class="align-middle">{{ training.getTrainingStartingHour()|date('H:i') }}</td>
                <td class="align-middle">{{ training.getTrainingEndingHour()|date('H:i') }}</td>
                <td class="align-middle">{{ listData.getTrainingType(training.getTrainingType()) }}</td>
                <td class="align-middle">{{ training.getTrainingAddress().getTrainingAddressName() }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% endif %}
    {% if clubTools.getLessonsDetails().AFA_teachers != null %}
    <h2>Professeur(s) & Assistant(s) AFA</h2>
    <table class="table table-sm table-hover table-striped">
        <thead>
            <tr>
                <th scope="col">Titre</th>
                <th scope="col">Type</th>
                <th scope="col">Prénom</th>
                <th scope="col">Nom</th>
                <th scope="col">Grade</th>
                <th scope="col">Enseignement Aïkikaï</th>
                <th scope="col">Enseignement ADEPS</th>
            </tr>
        </thead>
        <tbody>
        {% for teacher in clubTools.getLessonsDetails().AFA_teachers %}
            <tr>
                <td class="align-middle">{{ listData.getTeacherTitle(teacher.Title) }}</td>
                <td class="align-middle">{{ listData.getTeacherType(teacher.Type) }}</td>
                <td class="align-middle">{{ teacher.Firstname|title }}</td>
                <td class="align-middle">{{ teacher.Name|title }}</td>
                <td class="align-middle">{{ listData.getGrade(teacher.Grade) }}</td>
                <td class="align-middle">{% if teacher.GradeTitleAikikai != null %}{{ listData.getGradeTitle(teacher.GradeTitleAikikai) }}{% else %}Aucun{% endif %}</td>
                <td class="align-middle">{% if teacher.GradeTitleAdeps != null %}{{ listData.getGradeTitle(teacher.GradeTitleAdeps) }}{% else %}Aucun{% endif %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% endif %}
    {% if clubTools.getLessonsDetails().Foreign_teachers != null %}
    <h2>Professeur(s) & Assistant(s) Autre</h2>
    <table class="table table-sm table-hover table-striped">
        <thead>
            <tr>
                <th scope="col">Titre</th>
                <th scope="col">Type</th>
                <th scope="col">Prénom</th>
                <th scope="col">Nom</th>
                <th scope="col">Grade</th>
                <th scope="col">Enseignement Aïkikaï</th>
                <th scope="col">Enseignement ADEPS</th>
            </tr>
        </thead>
        <tbody>
        {% for teacher in clubTools.getLessonsDetails().Foreign_teachers %}
            <tr>
                <td class="align-middle">{{ listData.getTeacherTitle(teacher.Title) }}</td>
                <td class="align-middle">{{ listData.getTeacherType(teacher.Type) }}</td>
                <td class="align-middle">{{ teacher.Firstname|title }}</td>
                <td class="align-middle">{{ teacher.Name|title }}</td>
                <td class="align-middle">{{ listData.getGrade(teacher.Grade) }}</td>
                <td class="align-middle">{% if teacher.GradeTitleAikikai != null %}{{ listData.getGradeTitle(teacher.GradeTitleAikikai) }}{% else %}Aucun{% endif %}</td>
                <td class="align-middle">{% if teacher.GradeTitleAdeps != null %}{{ listData.getGradeTitle(teacher.GradeTitleAdeps) }}{% else %}Aucun{% endif %}</td>
            </tr>
        </tbody>
        {% endfor %}
    </table>
    {% endif %}
{% endblock %}

{% block javascripts %}
{% endblock %}
